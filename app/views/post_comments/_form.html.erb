<div class="mt-3">
<%= form_with model: [@book1, @post_comment], local: false , id: "comment_form" do |f| %>
  <div class="form-group">
    <%= f.text_area :comment, rows: 5, placeholder: "コメントをここに", class: "form-control", id: "comment_body" %>
  </div>
  <%= f.submit "送信する", class: "btn btn-primary mt-2" %>
<% end %>
</div>